[core]
    pager = delta
[delta]
    navigate = true
    side-by-side = true
[interactive]
    diffFilter = delta --color-only
[user]
	name = David Jacka
	email = ahoy.dave@gmail.com

[includeIf "gitdir:~/repos/"]
    path = ~/.gitconfig-work
[includeIf "gitdir:~/studio/"]
    path = ~/.gitconfig-work
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
